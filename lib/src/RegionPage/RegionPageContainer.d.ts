import React from 'react';
import { DatasetId } from '../../dataset-metadata/metadata';
type Props = {
    datasetId: DatasetId;
    regionId: string;
};
declare const RegionPageContainer: ({ datasetId, regionId }: Props) => React.JSX.Element;
export default RegionPageContainer;
