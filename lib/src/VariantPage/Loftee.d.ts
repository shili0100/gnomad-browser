import React from 'react';
type LofteeFilterProps = {
    filter: string;
};
export declare const LofteeFilter: ({ filter }: LofteeFilterProps) => React.JSX.Element;
type LofteeFlagProps = {
    flag: string;
};
export declare const LofteeFlag: ({ flag }: LofteeFlagProps) => React.JSX.Element;
export {};
